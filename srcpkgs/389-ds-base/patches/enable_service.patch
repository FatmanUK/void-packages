--- src/lib389/lib389/instance/setup.py	2021-04-07 08:46:28.000000000 +0100
+++ src/lib389/lib389/instance/setup.py	2021-05-17 20:56:58.659026552 +0100
@@ -807,7 +807,7 @@
         # If we are on the correct platform settings, systemd
         if general['systemd']:
             # Should create the symlink we need, but without starting it.
-            result = subprocess.run(["systemctl", "enable", "dirsrv@%s" % slapd['instance_name']],
+            result = subprocess.run(["ln", "-s", "/etc/sv/dirsrv", "/var/service/dirsrv-%s" % slapd['instance_name']],
                                     stdout=subprocess.PIPE, stderr=subprocess.PIPE)
             args = ' '.join(ensure_list_str(result.args))
             stdout = ensure_str(result.stdout)
